<Window x:Class="FloorplanAnnotator.Views.CreateProjectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Project"
        Width="520"
        Height="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,8,0" />
        </Style>
        <Style x:Key="BrowseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="80" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="Margin" Value="6,0,0,0" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Project Name -->
        <TextBlock Grid.Row="0" Text="Project Name" Style="{StaticResource LabelStyle}" Margin="0,0,0,4" />
        <TextBox Grid.Row="1"
                 Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}"
                 Padding="4"
                 Margin="0,0,0,12" />

        <!-- Floorplan File -->
        <TextBlock Grid.Row="2" Text="Floorplan Image" Style="{StaticResource LabelStyle}" Margin="0,0,0,4" />
        <DockPanel Grid.Row="3" Margin="0,0,0,12">
            <Button DockPanel.Dock="Right"
                    Content="Browse..."
                    Command="{Binding BrowseFloorplanCommand}"
                    Style="{StaticResource BrowseButtonStyle}" />
            <TextBox Text="{Binding FloorplanPath, UpdateSourceTrigger=PropertyChanged}"
                     Padding="4"
                     IsReadOnly="True"
                     Background="#F9F9F9"
                     ToolTip="{Binding FloorplanPath}" />
        </DockPanel>

        <!-- Annotations File -->
        <TextBlock Grid.Row="4" Text="Annotations File (JSON)" Style="{StaticResource LabelStyle}" Margin="0,0,0,4" />
        <DockPanel Grid.Row="5" Margin="0,0,0,12" VerticalAlignment="Top">
            <Button DockPanel.Dock="Right"
                    Content="Browse..."
                    Command="{Binding BrowseAnnotationsCommand}"
                    Style="{StaticResource BrowseButtonStyle}" />
            <TextBox Text="{Binding AnnotationsPath, UpdateSourceTrigger=PropertyChanged}"
                     Padding="4"
                     IsReadOnly="True"
                     Background="#F9F9F9"
                     ToolTip="{Binding AnnotationsPath}" />
        </DockPanel>

        <!-- Buttons + Error -->
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="{Binding ErrorMessage}"
                       Foreground="Red"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"
                       Visibility="{Binding HasError, Converter={StaticResource BoolToVisConverter}}" />

            <Button Grid.Column="1"
                    Content="Create"
                    Command="{Binding CreateCommand}"
                    Width="80"
                    Padding="4,6"
                    Margin="0,0,8,0"
                    IsDefault="True" />

            <Button Grid.Column="2"
                    Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Width="80"
                    Padding="4,6"
                    IsCancel="True" />
        </Grid>
    </Grid>
</Window>
